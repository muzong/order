<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.orderpay.order.infrastructure.mapper.OrderMapper">

    <resultMap id="BaseResultMap" type="com.orderpay.order.infrastructure.entity.OrderDO">
        <id column="id" property="id" />
        <result column="order_no" property="orderNo" />
        <result column="user_id" property="userId" />
        <result column="total_amount" property="totalAmount" />
        <result column="status" property="status" />
        <result column="pay_timeout_at" property="payTimeoutAt" />
        <result column="created_at" property="createdAt" />
        <result column="updated_at" property="updatedAt" />
        <result column="is_deleted" property="isDeleted" />
        <result column="create_by" property="createBy" />
        <result column="update_by" property="updateBy" />
    </resultMap>

    <select id="findById" parameterType="long" resultMap="BaseResultMap">
        SELECT * FROM orders 
        WHERE id = #{id} AND is_deleted = 0
    </select>

    <select id="findByOrderNo" parameterType="string" resultMap="BaseResultMap">
        SELECT * FROM orders 
        WHERE order_no = #{orderNo} AND is_deleted = 0
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" parameterType="com.orderpay.order.infrastructure.entity.OrderDO">
        INSERT INTO orders (
            order_no, user_id, total_amount, status, 
            pay_timeout_at, created_at, updated_at, is_deleted
        ) VALUES (
            #{orderNo}, #{userId}, #{totalAmount}, #{status},
            #{payTimeoutAt}, NOW(), NOW(), 0
        )
    </insert>

    <update id="update" parameterType="com.orderpay.order.infrastructure.entity.OrderDO">
        UPDATE orders SET
            updated_at = NOW(),
            status = #{status}
        WHERE id = #{id}
    </update>

</mapper>
